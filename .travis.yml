language: cpp

before_install:
  - cp makefiles/makefile.defs.linux.pulse ./makefile.defs

script:
  - make
  - make test

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - libgtk2.0-dev
    - libasound2-dev
    - libpulse-dev
